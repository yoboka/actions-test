{
  "name": "github-actions-test",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "clean": "rm -rf node_modules",
    "bootstrap": "npm i",
    "build": "rm -rf dist/ && webpack && copyfiles ./template/*.html dist -f"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "copyfiles": "^2.1.1",
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1"
  }
}
